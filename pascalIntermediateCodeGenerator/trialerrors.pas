program example(input, output;
var x,y: integer;
function gcd(a b:intege):integer;
begin
	if b = 0 then gcd:=a
	else gcd := gcd(b, a mod b)
end;

begin
	read x, y);
	a:=bc;
	a:=2a;
	b=2b;
	a:=b:=c;
	write(gcd(x y))
end.
